export enum Role {
    Admin = 'Admin',
    User = 'User',
}

export enum TaskStatus {
    Created = 'Created',
    InProgress = 'InProgress',
    Completed = 'Completed'
}

export enum TaskPriority {
    Low = 'Low',
    Medium = 'Medium',
    High = 'High'
}